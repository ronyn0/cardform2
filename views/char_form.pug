extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div
      label(for='name') Name:
      input#name(type='text', placeholder='Bob' name='name')
    button(type='submit') Submit

  if errors
   ul
    for error in errors
     li!= error.msg